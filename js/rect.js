/****************************** Adeil *********************************************/
function perimetr(length,width){
	//Вычисляем периметр прямоугольника длиной length и шириной width
	return length*2+width*2;
	
}
/****************************** Adeil *********************************************/
function rectArea(length,width){
//Вычисляем площадь прямоугольника длиной length и шириной width
	return length*width;
}	